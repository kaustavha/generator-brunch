language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-brunch' ]; then cd .. && eval "mv $currentfolder generator-brunch" && cd generator-brunch; fi

